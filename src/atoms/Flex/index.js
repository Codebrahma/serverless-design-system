import styled from 'styled-components';
import {
  alignItems,
  border,
  borderColor,
  color,
  display,
  flexDirection,
  flexWrap,
  justifyContent,
  order,
  space,
  width,
} from 'styled-system';

const List = styled.div`
  ${alignItems}
  ${border}
  ${borderColor}
  ${color}
  ${display}
  ${flexDirection}
  ${flexWrap}
  ${justifyContent}
  ${order}
  ${space}
  ${width}
`;

export default List;
