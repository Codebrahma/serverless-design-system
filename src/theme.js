const serverlessRed = '#fd5750';
const darkServerlessRed = '#cb0a02';
const white = '#ffffff'
const colors = {
  serverlessRed,
  darkServerlessRed,
  white,
};
export { colors }
const theme = {
  colors
}
export default theme;